# Porta do servidor
PORT=5501

# Remetente padrão (opcional)
MAIL_FROM=nexustinfo@gmail.com

# Opção 1: SMTP genérico
# SMTP_HOST=smtp.seuprovedor.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=usuario
# SMTP_PASS=senha

# Opção 2: Gmail com App Password
GMAIL_USER=nexustinfo@gmail.com
GMAIL_PASS=COLOQUE_APP_PASSWORD_AQUI
# Example .env for Nexus contact server
PORT=3000
DEFAULT_RECIPIENT=contato@nexusit.com
ALLOWED_RECIPIENTS=contato@nexusit.com,member1@nexusit.com

# SMTP settings (if you have an SMTP provider)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=user@example.com
SMTP_PASS=password
SMTP_FROM="Nexus Contact <no-reply@nexusit.com>"

# Misc
RATE_LIMIT_MAX=10
MOCK_EMAIL=true

# Optional reCAPTCHA (set your site secret to enable it)
RECAPTCHA_SECRET=
